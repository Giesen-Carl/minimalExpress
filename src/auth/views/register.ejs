<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="styles.css">
    <title>Login</title>
</head>

<body>
    <form action="/register<%= locals.redirect %>" method="POST">
        <label for="username">Username:</label>
        <input type="text" id="username" name="username" value="<%= locals.username %>" required>
        <br>
        <label for="password">Password:</label>
        <input type="text" id="password" name="password" value="<%= locals.password %>" required>
        <br>
        <label for="password">Password wiederholen:</label>
        <input type="text" id="password" name="passwordRepeat" value="<%= locals.passwordRepeat %>" required>
        <br>
        <input type="submit" value="Login">
    </form>
    <p>
        <%= locals.error %>
    </p>
</body>

</html>